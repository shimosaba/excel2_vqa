flowchart TB

subgraph LEGEND["凡例"]
direction LR
LG1["新規"]
LG2["変更"]
LG3["既存のまま"]
LG4["成果物/データ"]
end

subgraph BUILD["build-rulebook: 業務Excel → rulebook.json"]
direction TB

XLSX[("業務Excel<br/>1シート1000行超・図形/画像多数")]

subgraph PATHS["入力分解: シートごとに3経路"]
direction LR

subgraph VIS["視覚経路"]
direction TB
LO["LibreOffice PDF化<br/>SinglePageSheets: 1シート=1ページ<br/>実測: 高さ12,862.5pt上限で一様縮小<br/>クリップ無し・ベクタ保持"]
BANDIMG["pdftoppm 領域レンダ -y/-H<br/>DPI自動決定: 目標24〜45px/行<br/>行→px写像は行高累積で計算<br/>巨大PNG非経由"]
LO --> BANDIMG
end

subgraph CELLS["セル経路: openpyxl data_only"]
direction TB
GRID["グリッドTSV バンドスライス<br/>ヘッダ行を毎バンド再掲<br/>交差する結合セル・塗り色付き<br/>300行キャップ撤廃→バンド単位上限"]
end

subgraph DRAWP["図形経路: zip直読み"]
direction TB
DXML["xl/drawings/*.xml 直パース<br/>図形・テキストボックス文字列<br/>+アンカー行<br/>レンダ縮小の影響を受けない"]
MEDIA["xl/media/*<br/>埋め込み画像+アンカー行"]
end
end

XLSX --> LO
XLSX --> GRID
XLSX --> DXML
XLSX --> MEDIA

FIG["figureパス<br/>大きい埋め込み画像<br/>フロー図・別表画像など<br/>を個別に同スキーマ抽出"]

subgraph BAND["バンド処理: band_rows=150 + overlap=20"]
direction TB
ASM["バンド入力の組み立て<br/>領域画像 + グリッドスライス<br/>+ 図形テキスト アンカー行がバンド内<br/>+ 小さい埋め込み画像"]
EXT["VLM抽出 guided JSON<br/>→ SheetExtraction<br/>プロンプト改訂: バンド行範囲を明示<br/>priority未明記=0 sentinel"]
ASM --> EXT
end

BANDIMG --> ASM
GRID --> ASM
DXML --> ASM
MEDIA -->|"小図: バンドに添付"| ASM
MEDIA -->|"大図"| FIG

MERGE["機械的マージ merge_rules.py<br/>シグネチャdedupe: バンド重複を吸収<br/>sentinel priorityを出典行順で機械採番<br/>明記/未明記の混在シート→conflict<br/>同条件・異アクション→conflict"]

EXT --> MERGE
FIG --> MERGE

RB[("rulebook.json")]
MERGE --> RB
HUMAN["人間レビュー<br/>conflicts / cross_references<br/>/ unparsed_notes"]
MERGE -.-> HUMAN
HUMAN -.->|"修正"| RB
end

subgraph ROUTE["route / route-batch: 名刺 → RouteDecision 変更なし"]
direction TB
CARD[("名刺画像")]
CF["VLM抽出 guided<br/>CardFields 印字のまま"]
NORM["正規化 normalize.py<br/>NFKC・法人格・空白・casefold"]
ENG["決定的エンジン 三値評価<br/>適用ルール全列挙→priority解決<br/>blocking-UNKNOWNのみエスカレーション"]
JUDGE["LLM judge<br/>ルールブックをsystemに固定配置<br/>→ prefix caching有効"]
DEC[("RouteDecision<br/>selected_rule / abstain<br/>/ confidence / decided_by")]

CARD --> CF --> NORM --> ENG
ENG -->|"decided"| DEC
ENG -->|"tie: 勝者集合のみ"| JUDGE
ENG -->|"UNKNOWN候補"| JUDGE
ENG -->|"no_match 既定: 全ルール"| JUDGE
JUDGE --> DEC
end

RB --> ENG
RB -.->|"system prompt"| JUDGE

VLLM["vLLM: Qwen3.5-27B ネイティブVL<br/>--reasoning-parser qwen3<br/>--enable-prefix-caching"]
EXT -.-> VLLM
FIG -.-> VLLM
CF -.-> VLLM
JUDGE -.-> VLLM

EVAL["evaluate.py<br/>risk-coverage / reject_recall"]
DEC -.-> EVAL

classDef new fill:#fff3bf,stroke:#e8590c,stroke-width:2px,color:#000
classDef mod fill:#d0ebff,stroke:#1971c2,stroke-width:2px,color:#000
classDef keep fill:#e9ecef,stroke:#868e96,color:#000
classDef data fill:#d3f9d8,stroke:#2b8a3e,color:#000

class DXML,MEDIA,FIG,ASM,LG1 new
class BANDIMG,GRID,EXT,MERGE,LG2 mod
class LO,CF,NORM,ENG,JUDGE,VLLM,EVAL,HUMAN,LG3 keep
class XLSX,RB,CARD,DEC,LG4 data
